<section id="schedule" class="section schedule">
    <div class="container">
        <h2 class="section-title">Course Schedule</h2>
        
        <div class="schedule-table-wrapper">
            <table class="schedule-table">
                <thead>
                    <tr>
                        <th>Week</th>
                        <th>Type</th>
                        <th>Topic</th>
                        <th>Materials</th>
                        <th>Assessment</th>
                    </tr>
                </thead>
                <tbody>
                    {{#each lectures.lectures}}
                    <tr class="{{#if (eq type 'Midterm')}}midterm-row{{/if}}">
                        <td data-label="Week">{{week}}</td>
                        <td data-label="Type"><span class="badge badge-{{type}}">{{type}}</span></td>
                        <td data-label="Topic">
                            <strong>{{title}}</strong>
                            {{#if topics}}
                            <ul class="topic-list">
                                {{#each topics}}
                                <li>{{this}}</li>
                                {{/each}}
                            </ul>
                            {{/if}}
                        </td>
                        <td data-label="Materials">
                            {{#if materials}}{{materials}}{{/if}}
                            {{#if (eq week 1)}}
                            {{#if slides}}
                            <br><a href="{{slides}}" class="notebook-link" target="_blank" rel="noopener">📑 Slides</a>
                            {{/if}}
                            {{#if notebook}}
                            <br><a href="{{notebook}}" class="notebook-link" download>📓 Notebook</a>
                            {{/if}}
                            {{#if (eq title "Data Visualization Practice")}}
                            <br><a href="tur7-visualization.html" class="notebook-link" target="_blank" rel="noopener">📊 Xem tất cả bài tập</a>
                            <br><a href="notebook/Tur7_Visualization_Answer_Key.ipynb" class="notebook-link" download>🔑 Answer Key</a>
                            {{/if}}
                            {{#if submission_link}}
                            <br><a href="{{submission_link}}" class="notebook-link" target="_blank" rel="noopener">📤 Nộp bài tập</a>
                            {{/if}}
                            {{#if answer_keys_link}}
                            <br><a href="{{answer_keys_link}}" class="answer-keys-link">🔑 Đáp án thi</a>
                            {{/if}}
                            {{#if visualization_page_link}}
                            <br><a href="{{visualization_page_link}}" class="visualization-link">✨ Xem tất cả bài tập</a>
                            {{/if}}
                            {{/if}}
                            {{#if (eq week 2)}}
                            {{#if slides}}
                            <br><a href="{{slides}}" class="notebook-link" target="_blank" rel="noopener">📑 Slides</a>
                            {{/if}}
                            {{#if notebook}}
                            <br><a href="{{notebook}}" class="notebook-link" download>📓 Notebook</a>
                            {{/if}}
                            {{/if}}
                            {{#if (eq week 3)}}
                            {{#if slides}}
                            <br><a href="{{slides}}" class="notebook-link" target="_blank" rel="noopener">📑 Slides</a>
                            {{/if}}
                            {{#if notebook}}
                            <br><a href="{{notebook}}" class="notebook-link" download>📓 Notebook</a>
                            {{/if}}
                            {{/if}}
                            {{#if (eq week 4)}}
                            {{#if slides}}
                            <br><a href="{{slides}}" class="notebook-link" target="_blank" rel="noopener">📑 Slides</a>
                            {{/if}}
                            {{#if notebook}}
                            <br><a href="{{notebook}}" class="notebook-link" download>📓 Notebook</a>
                            {{/if}}
                            {{/if}}
                        </td>
                        <td data-label="Assessment">{{assessment}}</td>
                    </tr>
                    {{/each}}
                </tbody>
            </table>
        </div>
        
        <div class="schedule-note">
            <p><strong>Note:</strong> Schedule is subject to change. Check course announcements for updates.</p>
        </div>
    </div>
</section>

